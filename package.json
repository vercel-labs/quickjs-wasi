{
  "name": "quickjs-wasm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "make",
    "build:ts": "tsc",
    "build": "pnpm run build:wasm && pnpm run build:ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "degenerator": "^5.0.1",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
