---
'quickjs-wasi': patch
---

Browser example: render guest values without executing guest code — captured intrinsics for `toString`/Map/Set iteration, brand-derived constructor names, descriptor-based property reads, and identity-based `Object.prototype` detection.
