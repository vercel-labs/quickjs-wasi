---
'quickjs-wasi': minor
---

Add `handle.identity` (heap-value identity for deduplication/cycle detection), `handle.toBoolean()`, and `vm.construct(ctor, ...args)` for invoking constructors with `new` from the host.
