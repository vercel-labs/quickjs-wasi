---
"quickjs-wasi": minor
---

Add `vm.getMemoryUsage()` method returning detailed memory statistics from the QuickJS runtime, including counts and sizes for atoms, strings, objects, properties, shapes, functions, arrays, and binary objects.
